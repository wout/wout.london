<!doctype html>
<html lang="en">
  <head>
    {% render 'head', metadata: site.metadata,
                      title: page.title,
                      description: site.metadata.description %}
  </head>
  <body>
    {{ content }}
  </body>
</html>
